# MySQL 面试题库（自测用，无答案）

> 题目覆盖：基础架构 → 存储引擎 → 索引（B+树）→ SQL优化 → 事务 → 锁 → MVCC → 日志 → 主从复制 → 集群高可用 → 分库分表 → 实战场景

---

## 一、基础架构

1. MySQL 的整体架构分为哪几层？每层的职责是什么？ -- 连接器、解析器、预处理器、优化器、执行器、存储引擎、结果封装 & 资源清理
2. 一条 SQL 语句从客户端发出到返回结果，经历了哪些步骤？ 
3. 查询缓存（Query Cache）是什么？MySQL 8.0 为什么将其移除？ 
-- 锁竞争和命中率低：sql语句空格不一样都不行。而且每次数据更新都会阻塞让缓存失效，同时查询时不好控制锁粒度。
4. 连接器的作用是什么？长连接和短连接各有什么优缺点？
5. 解析器（Parser）和预处理器分别做了什么工作？
6. 优化器是如何选择执行计划的？它有哪些局限性？
7. 执行器与存储引擎之间是如何交互的？

---

## 二、存储引擎

8. MySQL 支持哪些存储引擎？默认是哪个？ -- InnoDB
9. InnoDB 和 MyISAM 的核心区别有哪些？各适用于什么场景？ 
-- InnoDB：支持事务、行级锁、通过redo log/undo log实现崩溃恢复、聚蔟索引、外键约束、MVCC多版本并发控制。这些myisam都没有。
10. InnoDB 为什么支持事务而 MyISAM 不支持？
11. InnoDB 的表空间结构是怎样的？独立表空间和共享表空间有什么区别？
12. InnoDB 的页（Page）默认大小是多少？页的内部结构是怎样的？
13. Memory 存储引擎的特点是什么？适合哪些场景？
14. 如何在线切换一张表的存储引擎？有什么风险？

---

## 三、索引与 B+ 树

### B+ 树基础
15. 什么是 B 树？B+ 树和 B 树有什么区别？
16. 为什么 MySQL 选择 B+ 树而不是二叉树、红黑树或 Hash 索引？
17. B+ 树的阶数是如何确定的？InnoDB 中一棵 B+ 树大概能存多少行数据？
18. B+ 树的叶子节点和非叶子节点分别存储什么？
19. 为什么 B+ 树的叶子节点要用双向链表连接？

### 索引类型
20. 聚簇索引（Clustered Index）和非聚簇索引（Secondary Index）的区别是什么？
21. InnoDB 的主键索引和二级索引在 B+ 树中分别长什么样？
22. 什么是回表？什么情况下会触发回表？如何避免？
23. 什么是覆盖索引？请举例说明。
24. 联合索引的底层结构是怎样的？最左前缀匹配原则是什么？
25. 什么是索引下推（Index Condition Pushdown，ICP）？它优化了什么？
26. 全文索引（FULLTEXT）的原理是什么？与 LIKE 查询有何区别？
27. 哈希索引的原理是什么？它不支持哪些查询类型？

### 索引优化
28. 哪些情况下索引会失效？请尽可能多地列举。
29. 为什么对索引列使用函数或运算会导致索引失效？
30. 什么是前缀索引？如何选择合适的前缀长度？
31. 索引的选择性（Cardinality）是什么？为什么低选择性列不适合建索引？
32. 什么情况下 MySQL 会选择不使用索引进行全表扫描？
33. 如何分析一条 SQL 是否走了索引？`EXPLAIN` 的各字段含义是什么？
34. `EXPLAIN` 中 type 字段的值从好到差排列是什么顺序？
35. `EXPLAIN` 中 Extra 字段出现 `Using filesort` 和 `Using temporary` 分别意味着什么？

---

## 四、SQL 优化

36. `SELECT *` 有什么问题？为什么要指定查询列？
37. 大表的 `COUNT(*)` 为什么慢？InnoDB 和 MyISAM 有什么差别？
38. `ORDER BY` 是如何执行的？什么情况下会用到 filesort？如何优化？
39. `GROUP BY` 的执行原理是什么？如何优化慢的 `GROUP BY` 查询？
40. `LIMIT` 分页在偏移量很大时为什么会很慢？如何优化深度分页？
41. `JOIN` 的执行算法有哪几种（NLJ、BNL、Hash Join）？各自的适用场景是什么？
42. 为什么要避免在 `JOIN` 的关联字段上数据类型不一致？
43. 子查询和 `JOIN` 哪个性能更好？为什么？
44. `IN` 和 `EXISTS` 的区别是什么？分别适合什么场景？
45. `UNION` 和 `UNION ALL` 的区别是什么？
46. 如何优化一条执行很慢的 SQL？完整的排查思路是什么？
47. 什么是慢查询日志？如何开启和分析？
48. `pt-query-digest` 工具的作用是什么？

---

## 五、事务

49. 事务的 ACID 特性分别是什么？InnoDB 如何保证每一条？
50. MySQL 支持哪几种事务隔离级别？默认是哪个？
51. 脏读、不可重复读、幻读分别是什么？举例说明。
52. 可重复读（RR）隔离级别下，幻读问题真的被完全解决了吗？
53. 事务的隔离级别是如何实现的？底层依赖哪些机制？
54. 什么是长事务？长事务会带来哪些问题？如何避免？
55. 事务提交和回滚的底层流程分别是什么？
56. 隐式事务和显式事务有什么区别？`autocommit` 参数的作用是什么？

---

## 六、锁

### 基础锁概念
57. MySQL 的锁按粒度分为哪几种？各有什么优缺点？
58. 共享锁（S锁）和排他锁（X锁）分别是什么？如何加锁？
59. 意向锁（IS锁、IX锁）的作用是什么？为什么需要意向锁？
60. 什么是表锁？哪些情况下 InnoDB 会使用表锁？

### 行锁
61. InnoDB 的行锁是加在索引上的还是加在记录上的？
62. 记录锁（Record Lock）、间隙锁（Gap Lock）、临键锁（Next-Key Lock）分别是什么？
63. 间隙锁在什么隔离级别下生效？它的作用是什么？
64. 临键锁（Next-Key Lock）的范围是如何确定的？
65. 什么情况下间隙锁会退化为记录锁？
66. 如何查看当前 MySQL 的锁等待情况？

### 死锁
67. 什么是死锁？InnoDB 是如何检测和处理死锁的？
68. 常见的导致死锁的 SQL 场景有哪些？
69. 如何减少死锁的发生？有哪些预防手段？
70. `innodb_lock_wait_timeout` 和死锁检测机制有什么区别？

### 其他锁
71. 乐观锁和悲观锁的区别是什么？如何在 MySQL 中实现乐观锁？
72. MDL 锁（元数据锁）是什么？什么情况下会导致 MDL 锁等待？
73. `SELECT ... FOR UPDATE` 和 `SELECT ... LOCK IN SHARE MODE` 的区别是什么？

---

## 七、MVCC（多版本并发控制）

74. MVCC 的核心思想是什么？它解决了什么问题？
75. InnoDB 的每行数据都包含哪些隐藏列？各有什么作用？
76. undo log 在 MVCC 中扮演了什么角色？版本链是如何构建的？
77. ReadView 是什么？它包含哪些关键字段？
78. 在读已提交（RC）和可重复读（RR）隔离级别下，ReadView 的创建时机有什么不同？
79. MVCC 是如何判断一个数据版本对当前事务是否可见的？
80. MVCC 能完全避免加锁吗？哪些操作仍然需要锁？
81. undo log 的版本链是什么时候被清理的？purge 线程做了什么？

---

## 八、日志系统

### redo log
82. redo log 是什么？它的作用是什么？
83. redo log 和 binlog 的本质区别是什么？为什么需要两套日志？
84. redo log 的 WAL（Write-Ahead Logging）机制是什么？
85. redo log buffer 和 redo log file 的关系是什么？
86. `innodb_flush_log_at_trx_commit` 的三个值（0/1/2）分别意味着什么？
87. redo log 的循环写机制是怎样的？checkpoint 是什么？

### undo log
88. undo log 的作用有哪些？（事务回滚 + MVCC）
89. undo log 存储在哪里？与 redo log 的关系是什么？
90. undo log 的类型有哪些（insert undo / update undo）？

### binlog
91. binlog 的作用是什么？它有哪几种格式（statement/row/mixed）？各有什么优缺点？
92. binlog 是在什么时机写入的？与 redo log 的两阶段提交是什么？
93. 两阶段提交（2PC）是如何保证 binlog 和 redo log 一致性的？
94. `sync_binlog` 参数的含义是什么？设置为 1 有什么影响？
95. 如何用 binlog 进行数据恢复（point-in-time recovery）？
96. binlog 和 redo log 在崩溃恢复时分别扮演什么角色？

### 其他日志
97. 错误日志（Error Log）记录什么内容？
98. 慢查询日志（Slow Query Log）如何配置？`long_query_time` 参数是什么？
99. 通用查询日志（General Log）有什么作用？为什么不建议长期开启？
100. relay log 是什么？在主从复制中起什么作用？

---

## 九、主从复制

101. MySQL 主从复制的原理是什么？完整流程是怎样的？
102. 主从复制中的三个线程分别是什么？各自的职责是什么？
103. 主从复制的三种模式（异步/半同步/同步）各有什么优缺点？
104. 什么是半同步复制（Semi-sync Replication）？它能完全避免数据丢失吗？
105. 主从延迟的原因有哪些？如何监控和减少主从延迟？
106. 从库的 SQL 线程是单线程的吗？并行复制是如何实现的？
107. GTID（全局事务标识）是什么？相比传统的 binlog position 有什么优势？
108. 如果从库落后主库太多，如何快速重建从库？
109. 主从复制中出现数据不一致该如何修复？`pt-table-checksum` 和 `pt-table-sync` 是什么？
110. 读写分离是如何实现的？读写分离会带来什么问题？

---

## 十、高可用与集群

111. MySQL 高可用方案有哪些？各有什么优缺点？
112. MHA（Master High Availability）的原理是什么？故障切换流程是怎样的？
113. MGR（MySQL Group Replication）的原理是什么？与传统主从有什么区别？
114. MGR 的单主模式和多主模式有什么区别？
115. Galera Cluster 的原理是什么？
116. InnoDB Cluster 是什么？它与 MGR 的关系是什么？
117. MySQL 的主备切换有哪几种策略？如何避免脑裂？
118. 什么是双主（Master-Master）架构？有哪些风险？

---

## 十一、分库分表

119. 什么时候需要分库分表？分库和分表分别解决什么问题？
120. 垂直分库、垂直分表、水平分库、水平分表分别是什么？
121. 水平分表的分片键如何选择？选择不好会带来什么问题？
122. 常见的分片算法有哪些（范围分片、Hash分片、一致性Hash）？各有什么优缺点？
123. 分库分表后如何处理跨分片的 `JOIN` 查询？
124. 分库分表后如何保证全局唯一 ID？有哪些方案？
125. 分库分表后如何处理分布式事务？有哪些解决方案？
126. 分库分表后的分页查询如何实现？
127. 常用的分库分表中间件有哪些（ShardingSphere、MyCat）？各有什么特点？
128. 不停机如何完成分库分表数据迁移？

---

## 十二、InnoDB 内存结构

129. InnoDB Buffer Pool 的作用是什么？如何配置大小？
130. Buffer Pool 中的 LRU 链表是如何管理的？为什么要做冷热分离？
131. 什么是预读（Read-Ahead）？线性预读和随机预读分别是什么？
132. Change Buffer 的作用是什么？它在什么情况下会被使用？
133. Adaptive Hash Index（自适应哈希索引）是什么？如何工作？
134. Log Buffer 的作用是什么？它和 redo log file 如何交互？
135. `innodb_buffer_pool_instances` 参数的作用是什么？

---

## 十三、表设计与数据类型

136. 设计表时主键应该如何选择？为什么推荐使用自增主键？
137. UUID 作为主键有什么缺点？
138. `VARCHAR` 和 `CHAR` 的区别是什么？各适合什么场景？
139. `TEXT` / `BLOB` 类型存储时有什么需要注意的？
140. `DATETIME` 和 `TIMESTAMP` 的区别是什么？
141. 为什么不推荐使用 `NULL`？`NULL` 对索引有什么影响？
142. `ENUM` 类型的优缺点是什么？
143. 整型字段定义 `INT(10)` 和 `INT(11)` 有什么区别？
144. 表设计的三大范式是什么？什么时候应该反范式设计？
145. 如何设计一张支持树形结构（如评论回复）的表？

---

## 十四、运维与监控

146. 如何在不锁表的情况下对大表加索引？`pt-online-schema-change` 的原理是什么？
147. Online DDL 是什么？MySQL 5.6+ 的 Online DDL 有什么限制？
148. `OPTIMIZE TABLE` 的作用是什么？什么时候需要执行？
149. 如何定位 MySQL 的性能瓶颈？有哪些工具和命令？
150. `SHOW PROCESSLIST` 能看到什么？如何处理长时间运行的查询？
151. `information_schema` 和 `performance_schema` 分别有什么用？
152. 如何估算一张表的大小和行数？
153. MySQL 的参数调优有哪些关键参数？（`innodb_buffer_pool_size` / `max_connections` 等）
154. 如何做 MySQL 的备份？`mysqldump` 和 `xtrabackup` 有什么区别？
155. 如何快速恢复一个误删的表或误执行的 `UPDATE`？

---

## 十五、实战与综合场景

156. 一张千万级大表，如何安全地删除一半数据？
157. 如何处理 MySQL 的连接数打满问题？
158. 线上出现慢 SQL 导致数据库 CPU 飙升，完整的排查和处理流程是什么？
159. 如何保证缓存（Redis）与 MySQL 数据库的双写一致性？
160. 分布式场景下如何实现 MySQL 的分布式事务？2PC / TCC / Saga 各有什么优缺点？
161. 如何设计一个高并发订单系统的数据库表结构？
162. 秒杀场景下如何防止 MySQL 超卖？
163. 如何用 MySQL 实现一个简单的分布式锁？有什么缺点？
164. 数据库连接池的核心参数（最小连接数、最大连接数、超时时间）如何配置？
165. 如果让你从零设计一套 MySQL 分库分表 + 读写分离方案，你会如何考虑？

